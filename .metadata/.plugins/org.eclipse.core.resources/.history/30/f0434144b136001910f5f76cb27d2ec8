package com.iadtec.hackathon.service;

import static org.junit.Assert.assertEquals;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;

@RunWith(SpringRunner.class)
@SpringBootTest
public class PaisServiceTests {
	
	@Autowired 
	private PaisService paisService;
	
	@Test
	public void contextLoads() {
	}

	@Test 
	public void createUser() {
		Pais savedPais = paisService.create(getPais());
		Pais userFromDb = paisService.findById(savedPais.getId());
		assertEquals(savedPais.getNome(), userFromDb.getNome());
	}
	
	private Pais getPais() {
		Pais pais = new Pais((long)15,"Ucrânia");
		return pais;
	}
	
}
